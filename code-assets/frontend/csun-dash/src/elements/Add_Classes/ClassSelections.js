

function ClassSelections() {
    return (
        <div>
            <h2 style={sectionTitle}>Class Selections</h2>
        </div>
    )
}

export default ClassSelections;

const sectionTitle = {
    color: "#E31C25",
    textAlign: "center"
}