# pytest.ini

[pytest]
markers = 
    problem_def:a def that needs to handle invalid input better to avoid crashing

# ignores files listed below
addopts = --ignore=code-assets/backend/scripts/server_stress_test

#python_files = tests.py test_*.py